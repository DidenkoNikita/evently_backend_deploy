export interface Number {
  number: string;
}

export interface CheckNuber {
  number: Number;
}

export interface Id {
  user_id: number;
}

export interface User {
  id: number;
  name: string;
}

export interface Data {
  id: number;
  accessToken: string;
}